name: CI

on:
  push:
    branches: [ "pipeline" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ramonglobo
          password: Globo@123

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/arm64/v8
          tags: |
            ramonglobo/app-globo:latest
            ramonglobo/app-globo:${{ github.sha }}

      #### Processo de atualização automática com uso de token do git 

      # - name: Commit and push updated manifests
      #   run: |
      #     git config --global user.name "github-actions"
      #     git config --global user.email "github-actions@github.com"
      #     git checkout -b pipeline-deploy
      #     git add k8s/*.yaml
      #     git commit -m "Update manifests with image tag ${{ github.sha }}"
      #     git push https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/SelecaoGlobocom/ramon_pereira.git pipeline-deploy
    
      # - name: Create Pull Request
      #   uses: peter-evans/create-pull-request@v4
      #   with:
      #     token: ${{ secrets.GITHUB_TOKEN }}
      #     branch: pipeline-deploy
      #     title: "Update manifests with image tag ${{ github.sha }}"
      #     body: "PR para update dos manifestos de kubernetes de acordo com a nova imagem gerada na pipeline."

      - name: Logout from Docker Hub
        run: docker logout

      - name: Show Docker image and tag
        run: |
          echo "Imagem Docker gerada com SHA: ramonglobo/app-globo:${{ github.sha }}"